# Skopíruj tento súbor ako .env a vyplň skutočné hodnoty.
# .env sa nikdy necommituje do gitu.
# SECRET_KEY vygeneruješ:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=
DATABASE_URL=sqlite+aiosqlite:///./vocab.db
